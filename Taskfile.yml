version: "3"

tasks:
  default:
    desc: Render the slides
    cmds:
      - quarto render

  preview:
    desc: Preview the slides with live reload
    cmds:
      - quarto preview index.qmd

  check:
    desc: Validate the Quarto project
    cmds:
      - quarto check
